<script setup>
struct Props {
    name: String,
    tagline: String,
}

#[thebe::get]
pub fn handler() -> Props {
    Props {
        name: "Thebe".to_string(),
        tagline: "A compiler-driven full-stack Rust web framework using Single File Components.".to_string(),
    }
}
</script>

<head>
    <title>About {{ name }}</title>
    <meta name="description" content="{{ tagline }}" />
</head>

<div class="max-w-2xl mx-auto py-12">
    <h1 class="text-4xl font-extrabold text-slate-900 tracking-tight mb-4 text-center sm:text-left">About {{ name }}</h1>

    <p class="text-xl text-slate-600 leading-relaxed mb-10 text-center sm:text-left">
        {{ tagline }}
    </p>

    <div class="grid grid-cols-2 gap-4 mb-10">
        <StatCard :label="'Framework'" :value="name">
            <p class="text-xs text-slate-500 mt-1">Full-stack Rust SFC</p>
        </StatCard>
        <StatCard label="Rendering" value="SSR">
            <p class="text-xs text-slate-500 mt-1">Zero virtual DOM</p>
        </StatCard>
    </div>

    <div class="bg-white p-8 rounded-2xl shadow-sm border border-slate-200">
        <h2 class="text-lg font-semibold text-slate-900 mb-6 uppercase tracking-wider text-sm">Key Features</h2>
        <ul class="space-y-4">
            <li class="flex items-start">
                <span class="flex h-6 w-6 shrink-0 items-center justify-center rounded-full border border-indigo-200 bg-indigo-50 text-indigo-600 mr-4">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                </span>
                <span class="text-slate-700">Server-side rendering via Axum</span>
            </li>
            <li class="flex items-start">
                <span class="flex h-6 w-6 shrink-0 items-center justify-center rounded-full border border-indigo-200 bg-indigo-50 text-indigo-600 mr-4">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                </span>
                <span class="text-slate-700">Fine-grained client reactivity — no virtual DOM</span>
            </li>
            <li class="flex items-start">
                <span class="flex h-6 w-6 shrink-0 items-center justify-center rounded-full border border-indigo-200 bg-indigo-50 text-indigo-600 mr-4">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                </span>
                <span class="text-slate-700">Single File Components <code class="font-mono text-sm bg-slate-100 text-indigo-700 px-2 py-0.5 rounded ml-1">.trs</code></span>
            </li>
            <li class="flex items-start">
                <span class="flex h-6 w-6 shrink-0 items-center justify-center rounded-full border border-indigo-200 bg-indigo-50 text-indigo-600 mr-4">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                </span>
                <span class="text-slate-700 flex items-center">Zero config — just run <code class="font-mono text-sm bg-slate-100 text-indigo-700 px-2 py-0.5 rounded ml-2">thebe dev</code></span>
            </li>
        </ul>
    </div>
</div>
