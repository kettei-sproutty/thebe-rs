<script setup>
use rand::RngExt;

struct Props {
    title: String,
    count: u32,
}

#[thebe::get]
pub fn handler() -> Props {
  println!("Handling GET request for /");
  let mut rng = rand::rng();
  let random_number: u32 = rng.random();

  Props {
    title: "Thebe Counter".to_string(),
    count: random_number
  }
}

#[thebe::post]
pub fn handler_post() -> Props {
  let mut rng = rand::rng();
  let random_number: u32 = rng.random();

  Props {
    title: "Thebe Counter POST".to_string(),
    count: random_number
  }
}
</script>

<head>
  <title>{{ title }}</title>
  <meta name="description" content="A server-rendered reactive counter demo for Thebe." />
</head>

<script lang="ts">
let props = getProps<Props>();

function increment() {
    props.count += 1;
}

function decrement() {
    props.count -= 1;
}

function reset() {
    props.count = 0;
}
</script>

<main>
    <h1>{{ title }}, World!</h1>
    <nav>
        <a href="/about">About</a>
    </nav>
     <div class="controls">
      <button onclick="decrement">−</button>
      <button onclick="increment">+</button>
      <button onclick="reset">Reset</button>
    </div>
    <div style="display: flex; gap: 1rem; justify-content: center; margin-bottom: 2rem;">
      <p class="count">{{ count }}!!</p>
    </div>
</main>

<style>

main {
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-family: system-ui, sans-serif;
}

h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
}

.count {
    font-size: 4rem;
    font-weight: bold;
    margin: 1rem 0;
}

.controls {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 2rem;
}

button {
    height: 3rem;
    font-size: 1.5rem;
    border: 2px solid #1a1a1a;
    background: white;
    border-radius: 0.5rem;
    cursor: pointer;
}

button:hover {
    background: #1a1a1a;
    color: white;
}

nav a {
    color: #555;
    text-decoration: none;
    font-size: 0.9rem;
}

nav a:hover {
    text-decoration: underline;
}
</style>
